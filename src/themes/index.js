import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      light: "#FE8F8F",
      dark: "#FF5C58",
      main: "#f381a7"
    },
    secondary: {
      light: "#b2102f",
      main: "#ff1744",
      dark: "#ff4569"
    },
    background: {
      default: "#FCD2D1",
      paper: "#FFFFFF"
    }
  }
});
